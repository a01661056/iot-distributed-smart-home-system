# Frontend Dashboard

This frontend is a Next.js dashboard for monitoring and controlling the smart home system.

## Responsibilities

- Display live and historical system data
- Visualize environmental metrics and device state
- Show NFC access activity and movement detection logs
- Send remote control commands to the backend

## Development

Run:

```bash
npm install
npm run dev
```

The dashboard currently calls the backend at `http://localhost:8000`.
